import styled from "styled-components";

const SideBarContent = styled.aside`
  flex: 1;
`;

function SideBar() {
  return (
    <SideBarContent>
      SideBar
    </SideBarContent>
  )
}

export default SideBar